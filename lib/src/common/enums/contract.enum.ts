export enum ContractStatus {
  PENDING = 'pending',
  ACTIVE = 'active',
  EXPIRED = 'expired',
  TERMINATED = 'terminated',
}

export enum DocumentType {
  CONTRACT = 'contract',
  AMENDMENT = 'amendment',
  OTHER = 'other',
}
