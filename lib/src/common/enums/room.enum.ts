export enum RoomType {
  SHARED = 'shared',
  PRIVATE = 'private',
}

export enum RoomStatus {
  AVAILABLE = 'available',
  OCCUPIED = 'occupied',
  MAINTENANCE = 'maintenance',
  RESERVED = 'reserved',
}
